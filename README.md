<!-- Give this prompt to Antigravity AI: -->
Act as a Senior React Developer, modify the adminPanel page, by modifying doesn't mean change any CSS, it means that add also new-Arrival option in "Category" field and in " Show on Page" field in order of after KidWear and before the Collections. Also add multiple sub-images field in "productMedia" field and if owner upload only one main image so make it for all sub-images, don't leave sub-images empty if only one main image is uploaded.
